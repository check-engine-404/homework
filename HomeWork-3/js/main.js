// Задача 1.
// Напишите цикл, который выводит числа от 1 до 20, но пропускает те числа, которые делятся на 4 без остатка.

for (let i = 1; i <= 20; i++) {
  if (i % 4 !== 0) {
    console.log(i);
  }
}

// Задача 2.
// С помощью цикла for вычислите факториал числа (произведение всех чисел от 1 до заданного числа). Используйте prompt для получение начального значения.

const factorial = +prompt("Введите число, а Я вычислю его факториал.", 0);

let result = 1;

for (let i = 1; i <= factorial; i++) {
  result *= i;
}
alert(result);

// *Задача 3.
// Выведите в консоль шахматную доску размером 8x8, где символы чередуются (например, # и пробел). Начинайте с # в первой ячейке первой строки.
// Размер доски

//  Половину кода ниже подсмотрел и разобрал, поэтому эту ЗАДАЧУ НЕ ЗАСЧИТЫВАЙТЕ, она мне для примера!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!

// const size = 8;

// // Внешний цикл — по строкам доски
// for (let i = 0; i < size; i++) {
//     let row = ""; // строка для текущей строки доски
    
//     // Внутренний цикл — по столбцам
//     for (let j = 0; j < size; j++) {
//         // Если сумма индексов четная, ставим '#', иначе — пробел
//         if ((i + j) % 2 === 0) {
//             row += "#";
//         } else {
//             row += " ";
//         }
//     }
    
//     // Выводим текущую строку доски
//     console.log(row);
// }
